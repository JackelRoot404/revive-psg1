## What changed

## Why

## Safety impact

- [ ] The default remains `INSTALLER_MODE=scan_only`.
- [ ] No firmware, APK, key, signature, user report, or device identifier is committed.
- [ ] Hardware/release claims link to consented, redacted, reproducible evidence.
- [ ] I ran `npm run typecheck`, `npm test`, and `npm run build`.

## Validation
